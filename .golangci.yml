version: "2"

linters:
  enable:
  - errcheck
  - govet
  - staticcheck
  - ineffassign
  - unused
  - misspell 
  - unconvert
  - revive
  - gocritic
  - nilerr
  - nolintlint
  - wastedassign

linters-settings:
  misspell:
    locale: US

issues:
  exclude-dirs:
    - migrations

run:
  timeout: 5m
